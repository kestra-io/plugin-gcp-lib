name: Main

on:
  schedule:
    - cron: '0 4 * * 1,2,3,4,5'
  push:
    branches:
      - main
      - releases/*
    tags:
      - v*

  pull_request:
    branches:
      - main
      - releases/*

  workflow_dispatch:
    inputs:
      skip-test:
        description: 'Skip test'
        type: choice
        required: true
        default: 'false'
        options:
          - "true"
          - "false"
      kestra-version:
        description: 'Kestra version'
        required: false
        type: string
        default: ''

permissions:
  contents: write
  checks: write
  actions: read
  pull-requests: write
  packages: read

jobs:
  check:
    uses: kestra-io/actions/.github/workflows/plugins.yml@main
    with:
      skip-test: ${{ github.event.inputs.skip-test == 'true' }}
      kestra-version: ${{ github.event.inputs.kestra-version }}
      # This is a shared library, not a runnable plugin: there is no shadow jar to build and it must
      # not be doc-indexed as a plugin. Mirrors plugin-azure-lib / plugin-kubernetes-lib.
      generate-shadowjar: false
      skip-indexing: true
    secrets: inherit
